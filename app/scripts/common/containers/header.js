import React from "react"
import Bucket from "./bucket"

const Header = () => {
    return (
        <div className="header-panel">
            <Bucket />
        </div>
    );
};

export default Header;